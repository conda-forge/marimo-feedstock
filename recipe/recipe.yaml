schema_version: 1

context:
  name: marimo
  version: "0.16.5"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/marimo-${{ version }}.tar.gz
  sha256: 8f5939d3c4e67ff25f6cfeefe731971ed7f3346c20098034b923a24a0d7770d6

build:
  skip:
    - ppc64le
    - aarch64
  number: 0
  python:
    entry_points:
      - marimo = marimo._cli.cli:main

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
    - uv-build
  host:
    - python
    - pip
    - uv-build
  run:
    - python
    - click >=8.0,<9
    - jedi >=0.18.0
    - markdown >=3.6,<4
    - pymdown-extensions >=10.15,<11
    - pygments >=2.13,<3
    - tomlkit >=0.12.0
    - pyyaml >=6.0
    - uvicorn >=0.22.0,<0.36.0
    - starlette >=0.35.0,!=0.36.0
    - websockets >=14.2.0
    - if: match(python, ">=3.11")
      then: loro >=1.5.0
    - if: match(python, "<3.11")
      then: typing-extensions >=4.4.0
    - docutils >=0.16.0
    - psutil >=5.0
    - itsdangerous >=2
    - narwhals >=1.34.1
    - packaging
    - msgspec >=0.19.0

tests:
  - python:
      imports:
        - marimo
      pip_check: true
  - requirements:
      run:
        - pip
    script:
      - marimo --help

about:
  summary: A library for making reactive notebooks and apps
  license: Apache-2.0
  license_file:
    - LICENSE
    - marimo/third_party.txt
    - marimo/third_party_licenses.txt
  homepage: https://marimo.io
  repository: https://github.com/marimo-team/marimo

extra:
  recipe-maintainers:
    - mscolnick
    - akshayka
    - mfansler
